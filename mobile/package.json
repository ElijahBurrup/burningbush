{
  "name": "burningbush-mobile",
  "version": "2.2.1",
  "private": true,
  "description": "The native shell around the Burning Bush app. www/ is generated by `node bin/build.js --native` from the repo root and is not edited by hand.",
  "scripts": {
    "web": "cd .. && node bin/build.js --native",
    "sync": "npm run web && node bin/stamp-version.js && cap sync android",
    "apk": "npm run sync && node bin/apk.js"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/local-notifications": "^7.0.0",
    "@capacitor-community/speech-recognition": "^7.0.0",
    "capacitor-native-settings": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
